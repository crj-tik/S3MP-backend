## 1. 后端现状审计与迁移防护

- [x] 1.1 增加只读审计，报告无内部空间、多个活动空间、跨租户绑定、重复 namespace、无归属对象和未完成任务引用
- [x] 1.2 为应用唯一内部存储关系和 canonical namespace 增加数据库约束及迁移前置检查
- [x] 1.3 编写安全回填迁移：只补建可唯一证明归属的记录，冲突项进入隔离并生成审计事件
- [x] 1.4 增加迁移、重复执行、冲突隔离和不改写既有 provider key 的测试

## 2. 后端租户与应用原子存储模型

- [x] 2.1 在租户创建流程建立并返回以不可变 tenant slug 派生的默认存储根上下文
- [x] 2.2 重构应用创建仓储，在同一事务中创建应用主体、应用、Owner、授权代表和唯一内部存储记录
- [x] 2.3 在共享 profile、managed connection、唯一约束或内部存储创建失败时回滚完整应用事务
- [x] 2.4 为应用创建、列表和详情响应增加只读 storage 摘要，确保不暴露 Bucket、连接、凭据或物理 URL
- [x] 2.5 增加应用创建成功、故障回滚、重命名路径不变和跨租户隔离测试

## 3. 后端应用上下文文件、授权与配额接口

- [x] 3.1 增加按 application_id 解析唯一内部存储上下文的服务，并让 application principal 只能解析自身应用
- [x] 3.2 提供应用上下文的文件列举、详情、上传、下载、multipart 和对象变更路由，复用现有授权与 provider target 安全管线
- [x] 3.3 将公开文件授权输入改为 application_id 加可选 canonical directory prefix，并在内部解析空间键
- [x] 3.4 将新配额创建、调整和应用用量查询统一到 tenant/application scope，storage_space scope 保持只读迁移兼容
- [x] 3.5 删除 `POST /api/v1/storage_spaces` 及其管理权限入口，将必须暂留的旧文件路由标记 deprecated 并禁止改变应用归属
- [x] 3.6 增加应用上下文文件、授权、配额、API Key 自身约束和旧路由兼容测试

## 4. 后端契约、目录和验收门

- [x] 4.1 更新 OpenAPI 应用 storage 摘要、应用文件路由、应用目录授权和应用配额契约
- [x] 4.2 从 OpenAPI、权限目录和元数据目录删除公开创建/绑定/选择 storage space 的能力，保留明确标注的迁移枚举
- [x] 4.3 更新后端文档、契约快照和测试夹具，消除“用户显式初始化 storage space”的描述
- [x] 4.4 运行后端单元、HTTP、仓储、迁移、OpenAPI 一致性和共享 S3 集成测试并修复全部失败
- [x] 4.5 确认后端契约稳定且验收通过后，记录前端迁移所用 OpenAPI 版本；在此任务完成前不得开始第 5 组

## 5. 前端生成客户端与应用只读展示

- [x] 5.1 从第 4 组确认的 OpenAPI 重新生成前端类型和 API 客户端，并更新 Mock 契约
- [x] 5.2 删除全局“创建存储空间”入口、独立创建弹窗和相关 POST 调用
- [x] 5.3 删除应用详情中的创建/绑定/解绑存储空间操作及“暂无存储空间”业务状态
- [x] 5.4 在租户详情只读展示默认根路径，在应用创建结果、列表或详情只读展示路径、状态、用量和配额
- [x] 5.5 调整导航和实体链接，使内部 storage-space 详情仅在迁移/运维需要时可见，不作为普通业务一级资源

## 6. 前端文件、授权与配额流程迁移

- [x] 6.1 将文件浏览、直传、下载、multipart 和对象操作页面改为应用上下文，不再显示 storage-space 选择器
- [x] 6.2 将角色绑定和用户组文件授权表单改为选择应用及可选目录前缀
- [x] 6.3 将配额列表和应用详情改为 tenant/application 业务范围，历史 storage-space 行仅只读标识为迁移数据
- [x] 6.4 更新前端 Mock、路由、权限判断、单元测试和页面文案，清除创建、绑定或选择存储空间的交互

## 7. 跨仓库联调与发布验证

- [x] 7.1 联调创建租户 `sft-test`，验证默认根只读展示为 `sft-test`
- [x] 7.2 联调在该租户创建应用，验证无需额外操作即展示并使用 `sft-test/<application_id>`
- [x] 7.3 验证应用界面、文件流程、授权流程和配额流程均不存在 storage-space 创建、绑定或选择控件
- [x] 7.4 验证跨租户、跨应用、路径穿越、应用 Key 覆盖目标和迁移冲突均被拒绝或隔离
- [x] 7.5 运行前后端完整测试、构建和端到端场景，记录兼容路由退役条件与发布/回滚步骤
